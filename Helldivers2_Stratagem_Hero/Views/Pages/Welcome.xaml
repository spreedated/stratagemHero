<Page x:Class="Helldivers2_Stratagem_Hero.Views.Pages.Welcome"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" d:DataContext="{d:DesignInstance Type=local:Welcome}"
      xmlns:local="clr-namespace:Helldivers2_Stratagem_Hero.Views.Pages"
      xmlns:vl="clr-namespace:Helldivers2_Stratagem_Hero.ViewLogic"
      mc:Ignorable="d" 
      d:DesignHeight="372" d:DesignWidth="920"
      Title="Welcome">

    <Grid>
        <Canvas/> <!-- TODO: make background -->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" VerticalAlignment="Bottom" Margin="0,0,0,12" HorizontalAlignment="Center" Text="STRATAGEM HERO" FontSize="48"/>
            <TextBlock Grid.Row="1" VerticalAlignment="Top" Margin="0,64,0,0" HorizontalAlignment="Center" Text="Enter any Stratagem Input to Start!" Foreground="{StaticResource Yellow}"/>
        </Grid>
    </Grid>
</Page>
